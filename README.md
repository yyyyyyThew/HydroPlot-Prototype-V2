# HydroPlot-Prototype-V2
Takes csv/xlsx input and plots a really cool graph
## TO DO
-re-write code (full documentation)
-design UI properly (full documentation)
-add graph colour selection
-actually write the code for plotting (allow for range of resolutions)
-minimise the length of some sections of code e.g CurrentData.LocalColumn.RegressLog(CurrentData.LocalColumn.Data) -> logline
-fix naming of vars and methods
-make some of the buttons do something
-add functionality to new file button
